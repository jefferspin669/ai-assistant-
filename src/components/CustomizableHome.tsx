"use client";

import { CommandDashboard } from "@/components/CommandDashboard";

/** Retired widget home — CommandDashboard owns layout + DashboardCustomizer. */
export function CustomizableHome() {
  return <CommandDashboard />;
}
