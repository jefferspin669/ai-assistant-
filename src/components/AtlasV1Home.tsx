"use client";

import { CommandDashboard } from "@/components/CommandDashboard";

/** Retired home — CommandDashboard is the product dashboard. Kept as a wrapper. */
export function AtlasV1Home(_props?: { refreshKey?: number }) {
  return <CommandDashboard />;
}
